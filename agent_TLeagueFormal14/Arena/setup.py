from setuptools import setup

setup(
    name='Arena',
    version='0.1',
    description='Arena',
    keywords='Arena',
    packages=[
        'arena',
    ],

    install_requires=[
        'gym',
        'pillow'
    ],
)
